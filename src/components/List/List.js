// import PropTypes from 'prop-types';

export default function List({ savedTimes }) {
  return (
    <ul className="list">
      
    </ul>
  );
}

// List.propTypes = {
// props: PropTypes.number|string|bool|func|array|object|node.isRequired,
// };

// List.defaultProps = {
// props: '',
// };
